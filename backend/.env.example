# 数据源 (默认值，可被前端选择覆盖)
# Options: yahoo-finance (免费无限制), alpha-vantage (有API限制)
DATA_SOURCE=yahoo-finance

# AI Analysis Provider (Options: claude, openai, rule-based)
AI_PROVIDER=rule-based

# Claude API Key (required only if AI_PROVIDER=claude)
ANTHROPIC_API_KEY=your_claude_api_key_here

# OpenAI API Key (required only if AI_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model (optional, defaults to gpt-4o)
# Options: gpt-4o, gpt-4-turbo, gpt-4, gpt-3.5-turbo, or any future models
OPENAI_MODEL=gpt-4o

# Alpha Vantage API Key (required for stock data)
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key_here

# Server Port
PORT=3001
